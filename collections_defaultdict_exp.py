from collections import defaultdict

#building default dict
dDict = defaultdict(list)   #decault variable type can be anything
dDict['a'] = [1,2,3]
dDict['b'] = [4,5,6]
print(dDict)
print(dDict['c'])
#
